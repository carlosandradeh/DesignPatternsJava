package patronesComportamiento.iterator;

/** Interfaz que deine el comportamiento de un Iterable. */
public interface Iterable {
    /**
     * Método que retorna un iterador para iterar el iterable.
     * 
     * @return Iterator.
     */
    public Iterator iterator();
}
